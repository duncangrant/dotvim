Installation:

  git clone git://github.com/duncangrant/dotvim.git ~/.vim

Create symlinks:

  ln -s ~/.vim/vimrc ~/.vimrc

Switch to '~/.vim' and fetch submodules

  cd ~/.vim
  git submodule init
  git submodule update

Adding a plugin as a submodule

  cd ~/.vim
  mkdir ~/.vim/bundle
  git submodule add http://github.com/tpope/vim-fugitive.git bundle/fugitive
  git add .
  git commit -m "Install Fugitive.vim bundle as a submodule."

Plugins that I should probably add back in

  nerdtree
  indentanything
  taglist-plus - add javascript to taglist
  taglist
  tortoise typing
  vcs command - already have fugitive for git
  vjde - ??
