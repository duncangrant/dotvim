Installation:

  git clone git://github.com/duncangrant/dotvim.git ~/.vim

Create symlinks:

  ln -s ~/.vim/vimrc ~/.vimrc

Switch to '~/.vim' and fetch submodules

  cd ~/.vim
  git submodule init
  git submodule update

